{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["app/components/*"] // Update to point to the correct location of components
    }
  },
  "include": ["app/**/*", "**/*.ts", "**/*.tsx"], // Ensure both app and components are included
  "exclude": ["node_modules"]
}
